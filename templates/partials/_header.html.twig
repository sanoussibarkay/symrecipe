<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home.index') }}">Accueil</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
          <li class="nav-item">
          <a class="nav-link active" href="{{ path('recipe.community') }}">Recettes communautaires
          </a>
        </li>
        {% if app.user %}
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_ingredient') }}">Mes Ingredient
          </a>
        </li>
          <li class="nav-item">
          <a class="nav-link active" href="{{ path('recipe.index') }}">Mes Recettes
          </a>
        </li>
        {% endif %}
      {% if app.user %}
         <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullname }}</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="{{ path('user.edit' , {'id': app.user.id}) }}">Modifier mon compte</a>
            <a class="dropdown-item" href="{{ path('user.edit.password', {'id': app.user.id}) }}">modifier mon mot de passe</a>
             <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('security.logout') }}"> Logout </a>
           

          </div>
        </li>
         {% else %}
         <li class="nav-item">
          <a class="nav-link active" href="{{ path('security.login') }}">Connexion

          </a>
        </li>
           <li class="nav-item">
          <a class="nav-link active" href="{{ path('security.registration') }}">Inscription

          </a>
        </li>
        {% endif %}




           
      </ul>
    
    </div>
  </div>
</nav>

